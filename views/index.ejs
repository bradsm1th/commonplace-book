<%- include('./partials/header')%>

  <!-- show only if authenticated -->
  <% if (user) { %>
    <p class="text-center">You're logged in! <span class="text-white bg-info">(<%= user.email %>)</span></p>
  <% } else { %>
    <p class="text-center">Why not <a href="/auth/google">log in</a>?</p>
    <% } %>
<%- include('./partials/footer')%>